# PCL Normal Distributions Transform Demo

Point Cloud Library (PCL) Normal Distributions Transform (NDT) Demo.


## Build and Run

Follow the commands below in `src` directory to build and run the demo.

```
cmake .
make
./ndt
```

